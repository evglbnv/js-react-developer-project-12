{
  "name": "@hexlet/code",
  "version": "1.0.0",
  "description": "chat project",
  "main": "index.js",
  "scripts": {
    "build": "npm run build --prefix frontend",
    "start": "start-server -p 5000 -s ./frontend/build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Evgeniy LOBANOV",
  "license": "ISC",
  "dependencies": {
    "@hexlet/chat-server": "^1.1.8"
  }
}